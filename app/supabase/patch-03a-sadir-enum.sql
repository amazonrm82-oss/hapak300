-- ═══════════════════════════════════════════════════════════════════════════
--  עדכון 3, חלק א׳ — פתיחת צוות "סדיר"
--
--  ⚠️ שתי הרצות נפרדות, בגלל מגבלה של Postgres: ערך חדש ברשימת הצוותים חייב
--  להישמר לפני שאפשר להשתמש בו. לכן:
--
--     1. הדבק את הקובץ הזה → Run.   (שורה אחת)
--     2. ואז את patch-03b            → Run.
--
--  אם תריץ את שניהם יחד תקבל: "New enum values must be committed before they
--  can be used" — וזה בדיוק מה שהפיצול הזה מונע.
-- ═══════════════════════════════════════════════════════════════════════════

alter type team_key add value if not exists 'c';
